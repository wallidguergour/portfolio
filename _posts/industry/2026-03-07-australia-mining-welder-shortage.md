---
layout: article
title: "Why Australia's Mining Boom Is Creating a Critical Shortage of Certified Welders"
description: "Australia faces a 70,000-welder shortfall by 2030. Mining vacancies exceed the 2011 boom. What it means for certified tradespeople targeting Australian mining."
date: 2026-03-07
category: industry
permalink: /blog/industry/australia-mining-welder-shortage-2025/
image: /images/blog/australia-mining-welder-shortage.webp
image_alt: "Conveyor system at an iron ore mine in Western Australia's Pilbara region"
image_caption: "Iron ore conveyor infrastructure in the Pilbara, WA. The equipment needs certified welders to build and maintain it. Australia does not have enough of them."
read_time: 8

tags:
  - Australia
  - Mining
  - Welder Shortage
  - Skilled Migration
  - TRA
  - Subclass 189
  - Western Australia

faq:
  - q: "How many welders does Australia need by 2030?"
    a: "Weld Australia projects a shortfall of at least 70,000 welders by 2030. This figure does not account for the additional demand generated by Australia's transition to renewable energy infrastructure, which will require tens of thousands of additional fabrication hours for wind towers, solar structures, and grid upgrades."
  - q: "Is welding on the Australian skills shortage list?"
    a: "Yes. Welder (ANZSCO 322313) is listed under the Core Skills Stream of the Skills in Demand visa (Subclass 482), which means Australian employers can sponsor overseas welders when they cannot find qualified local workers. The occupation has been on shortage lists consistently for several years."
  - q: "How much do welders earn in Australian mining?"
    a: "Welders and boilermakers in Australian mining earn between AUD 140,000 and AUD 200,000 annually, particularly in FIFO (fly-in fly-out) roles in Western Australia. Senior certified welders with specialist experience in high-strength steels or specific mining equipment can command rates at the upper end of that range or above on contract."
  - q: "Why are there so few qualified welders in Australia?"
    a: "Multiple factors compound the shortage: the welding workforce dropped 8% over several years while demand accelerated, apprenticeship completion rates are low, the existing workforce is aging, and young Australians increasingly avoid trades perceived as outdated or environmentally problematic. Immigration has historically not solved the gap because the global welder shortage is structural, not regional."
  - q: "Can an overseas welder migrate to Australia permanently?"
    a: "Yes. The Subclass 189 skilled independent visa allows qualified tradespeople to migrate permanently without employer sponsorship, provided they pass a skills assessment through TRA (Trades Recognition Australia) and meet the points threshold. Welding is on the Medium and Long-term Strategic Skills List, making it eligible for the 189 pathway."

howto:
  name: "How an Overseas Welder Qualifies for Australian Skilled Migration"
  description: "Overview of the TRA skills assessment and visa pathway for certified welders targeting the Australian mining market."
  steps:
    - name: "Get your trade recognised by TRA"
      text: "Trades Recognition Australia (TRA) assesses overseas welding qualifications against the Australian Certificate III in Engineering — Fabrication Technology. You submit employment references, qualification documents, and evidence of trade experience. TRA can assess your application from outside Australia."
    - name: "Sit the trade recognition assessment if required"
      text: "Depending on your qualifications and country of origin, TRA may require a practical trade assessment at an approved Australian assessment centre, or may grant recognition based on documentation alone. Candidates from countries with mutual recognition agreements have a streamlined process."
    - name: "Submit an Expression of Interest through SkillSelect"
      text: "Once you have a positive TRA assessment, you submit an Expression of Interest for the Subclass 189 visa through the SkillSelect system. Your points score is calculated based on age, English proficiency, qualifications, and years of experience. Welding is on the Medium and Long-term Strategic Skills List."
    - name: "Receive an invitation and lodge your visa application"
      text: "If your points score is competitive in an invitation round, you receive an invitation to apply and have 60 days to lodge the full application. The Subclass 189 grants permanent residence with no employer sponsorship requirement and no restriction on the type of work or employer."
---

Australia is the most resource-rich nation on earth. It holds 29% of global iron ore reserves, 39.8% of global lithium output, and contributes more than 12% of national GDP from mining alone. The Pilbara in Western Australia runs some of the largest open-pit iron ore operations ever built. BHP, Rio Tinto, Fortescue, and Roy Hill are not small companies — they are the backbone of a commodity export economy that represents roughly 70% of Australia's total export earnings.

All of that infrastructure needs to be built, maintained, and repaired. Conveyors, crushers, chutes, structural steel, pressure vessels, mobile equipment. Every metre of that equipment has welds in it, and every one of those welds needs to be made by someone who knows what they are doing.

Australia does not have enough of those people. Not even close.

## The numbers

<!-- ============================================================
     Visualisation : Dashboard — Pénurie de soudeurs en Australie
     Article : "Why Australia's Mining Boom Is Creating a Critical Shortage"
     Blog : wallid-guergour.com | Jekyll / GitHub Pages
     Pas de dépendance externe — HTML/CSS/JS pur
     Déclenchement : Intersection Observer (animation au scroll)
     ============================================================ -->

<div id="viz-stats" style="width:100%; max-width:720px; margin:32px auto;"></div>

<style>
#viz-stats {
  --bg:      #0B0F14;
  --card:    #0f161e;
  --accent:  #F2DBC2;
  --border:  rgba(255,255,255,0.07);
  --sub:     rgba(255,255,255,0.45);
  --danger:  #c85a5a;
  --warn:    #c8964a;
  --pos:     #5a9e72;
  font-family: 'Courier New', Courier, monospace;
  background: var(--bg);
  border: 1px solid var(--border);
  border-radius: 4px;
  overflow: hidden;
  box-sizing: border-box;
}

#viz-stats .vs-header {
  padding: 16px 22px 12px;
  border-bottom: 1px solid var(--border);
  display: flex;
  align-items: baseline;
  gap: 14px;
  flex-wrap: wrap;
}
#viz-stats .vs-header-label {
  font-size: 10px;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  color: var(--accent);
  font-weight: 700;
}
#viz-stats .vs-header-sub {
  font-size: 9.5px;
  color: var(--sub);
  font-family: Arial, sans-serif;
  letter-spacing: 0;
}

#viz-stats .vs-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 1px;
  background: var(--border);
}

#viz-stats .vs-cell {
  background: var(--card);
  padding: 18px 20px 16px;
  position: relative;
  overflow: hidden;
}

/* Cellule large (span 2) */
#viz-stats .vs-cell.wide {
  grid-column: span 2;
}

/* Numéro principal */
#viz-stats .vs-num {
  font-size: 46px;
  font-weight: 700;
  line-height: 1;
  letter-spacing: -2px;
  color: var(--accent);
  transition: color 0.3s;
}
#viz-stats .vs-num.danger { color: var(--danger); }
#viz-stats .vs-num.warn   { color: var(--warn);   }
#viz-stats .vs-num.pos    { color: var(--pos);    }

#viz-stats .vs-unit {
  font-size: 14px;
  letter-spacing: 0;
  margin-left: 2px;
  opacity: 0.7;
}

#viz-stats .vs-label {
  margin-top: 7px;
  font-size: 11px;
  color: rgba(255,255,255,0.8);
  font-family: Arial, sans-serif;
  line-height: 1.4;
  font-weight: 600;
}
#viz-stats .vs-source {
  margin-top: 5px;
  font-size: 9px;
  color: var(--sub);
  font-family: Arial, sans-serif;
  letter-spacing: 0.02em;
}

/* Barre de progression */
#viz-stats .vs-bar-track {
  margin-top: 10px;
  height: 3px;
  background: rgba(255,255,255,0.06);
  border-radius: 2px;
  overflow: hidden;
}
#viz-stats .vs-bar-fill {
  height: 100%;
  border-radius: 2px;
  width: 0%;
  transition: width 1.4s cubic-bezier(0.16, 1, 0.3, 1);
}

/* Cellule double valeur (avant/après) */
#viz-stats .vs-compare {
  display: flex;
  align-items: flex-end;
  gap: 10px;
  margin-top: 2px;
}
#viz-stats .vs-compare-item {
  display: flex;
  flex-direction: column;
}
#viz-stats .vs-compare-year {
  font-size: 8.5px;
  color: var(--sub);
  letter-spacing: 0.08em;
  margin-bottom: 2px;
  font-family: Arial, sans-serif;
}
#viz-stats .vs-arrow {
  font-size: 22px;
  color: var(--danger);
  margin-bottom: 6px;
  align-self: center;
  line-height: 1;
}

/* Décoration de fond */
#viz-stats .vs-cell::after {
  content: '';
  position: absolute;
  bottom: -12px; right: -12px;
  width: 60px; height: 60px;
  border: 1px solid rgba(255,255,255,0.03);
  border-radius: 50%;
  pointer-events: none;
}

/* Cellule pleine largeur : rangée de vacances */
#viz-stats .vs-vacancies {
  display: flex;
  gap: 0;
  align-items: stretch;
}
#viz-stats .vs-vac-item {
  flex: 1;
  padding: 18px 20px 16px;
  border-right: 1px solid var(--border);
}
#viz-stats .vs-vac-item:last-child { border-right: none; }

/* Animation d'entrée */
#viz-stats .vs-cell,
#viz-stats .vs-vac-item {
  opacity: 0;
  transform: translateY(8px);
  transition: opacity 0.5s ease, transform 0.5s ease;
}
#viz-stats.vs-visible .vs-cell,
#viz-stats.vs-visible .vs-vac-item {
  opacity: 1;
  transform: translateY(0);
}
</style>

<script>
(function() {

  const root = document.getElementById('viz-stats');

  // ── Données ────────────────────────────────────────────────────
  // Toutes les valeurs viennent de l'article (Weld Australia, AUSMASA, gov data)
  const stats = [
    {
      id: 'shortfall',
      num: 70000, suffix: '', unit: '',
      label: 'welder shortfall projected by 2030',
      source: 'Weld Australia',
      color: 'danger',
      bar: null,
      wide: false,
    },
    {
      id: 'workforce-drop',
      num: 8, suffix: '%', unit: '',
      label: 'drop in welding trade workers',
      source: 'Weld Australia — recent years',
      color: 'danger',
      bar: { pct: 8, max: 20, color: 'var(--danger)' },
      wide: false,
    },
    {
      id: 'skills-shortage',
      num: null, // cellule comparaison
      compare: [
        { year: '2021', val: 34, suffix: '%' },
        { year: '2022', val: 63, suffix: '%' },
      ],
      label: 'mining industry skills shortage — 2nd most acute in Australia',
      source: 'AUSMASA / National Skills Commission',
      color: 'warn',
      wide: false,
    },
    {
      id: 'workers-needed',
      num: 56000, suffix: '+', unit: '',
      label: 'additional mining workers needed by 2033',
      source: 'Australian Government workforce data',
      color: 'warn',
      bar: null,
      wide: false,
    },
  ];

  const vacancies = [
    { region: 'Queensland',       pct: 87,  color: 'var(--danger)' },
    { region: 'Western Australia', pct: 80,  color: 'var(--warn)'   },
    { region: 'WA workforce growth', pct: 40, color: 'var(--pos)', label: '40% of national resource workforce growth', suffix: '%' },
  ];

  // ── Construction DOM ───────────────────────────────────────────

  // En-tête
  const header = document.createElement('div');
  header.className = 'vs-header';
  header.innerHTML = `
    <span class="vs-header-label">Welder Shortage — Australia</span>
    <span class="vs-header-sub">Key figures from government data, Weld Australia &amp; AUSMASA</span>
  `;
  root.appendChild(header);

  // Grille principale
  const grid = document.createElement('div');
  grid.className = 'vs-grid';
  root.appendChild(grid);

  // Cellules stats
  stats.forEach((s, idx) => {
    const cell = document.createElement('div');
    cell.className = 'vs-cell' + (s.wide ? ' wide' : '');
    cell.style.transitionDelay = (idx * 80) + 'ms';

    if (s.compare) {
      // Cellule comparaison (avant / après)
      cell.innerHTML = `
        <div class="vs-compare" id="compare-${s.id}">
          <div class="vs-compare-item">
            <span class="vs-compare-year">${s.compare[0].year}</span>
            <span class="vs-num ${s.color}" data-target="${s.compare[0].val}" data-suffix="${s.compare[0].suffix}">0${s.compare[0].suffix}</span>
          </div>
          <div class="vs-arrow">→</div>
          <div class="vs-compare-item">
            <span class="vs-compare-year">${s.compare[1].year}</span>
            <span class="vs-num ${s.color}" data-target="${s.compare[1].val}" data-suffix="${s.compare[1].suffix}" style="font-size:56px">0${s.compare[1].suffix}</span>
          </div>
        </div>
        <div class="vs-bar-track">
          <div class="vs-bar-fill" data-pct="63" data-max="100" style="background:var(--${s.color})"></div>
        </div>
        <div class="vs-label">${s.label}</div>
        <div class="vs-source">${s.source}</div>
      `;
    } else {
      // Cellule standard
      const barHtml = s.bar
        ? `<div class="vs-bar-track">
             <div class="vs-bar-fill" data-pct="${s.bar.pct}" data-max="${s.bar.max}" style="background:${s.bar.color}"></div>
           </div>`
        : '';
      cell.innerHTML = `
        <div class="vs-num ${s.color}" data-target="${s.num}" data-suffix="${s.suffix}">0${s.suffix}</div>
        ${barHtml}
        <div class="vs-label">${s.label}</div>
        <div class="vs-source">${s.source}</div>
      `;
    }

    grid.appendChild(cell);
  });

  // Cellule vacances (pleine largeur)
  const vacCell = document.createElement('div');
  vacCell.className = 'vs-cell wide';
  vacCell.style.transitionDelay = (stats.length * 80) + 'ms';
  vacCell.style.padding = '0';

  const vacRow = document.createElement('div');
  vacRow.className = 'vs-vacancies';

  vacancies.forEach((v, i) => {
    const item = document.createElement('div');
    item.className = 'vs-vac-item';
    item.style.transitionDelay = ((stats.length + i) * 80) + 'ms';
    item.innerHTML = `
      <div class="vs-num warn" data-target="${v.pct}" data-suffix="%" style="color:${v.color}">0%</div>
      <div class="vs-bar-track">
        <div class="vs-bar-fill" data-pct="${v.pct}" data-max="100" style="background:${v.color}"></div>
      </div>
      <div class="vs-label">${v.label || 'welding vacancy increase'}</div>
      <div class="vs-source">${v.region}</div>
    `;
    vacRow.appendChild(item);
  });

  vacCell.appendChild(vacRow);
  grid.appendChild(vacCell);

  // ── Animation des compteurs ────────────────────────────────────
  function animateCounter(el, target, suffix, duration) {
    const start    = performance.now();
    const isLarge  = target >= 1000;
    // Easing : ease out expo
    function ease(t) { return t === 1 ? 1 : 1 - Math.pow(2, -10 * t); }

    function tick(now) {
      const elapsed = now - start;
      const progress = Math.min(elapsed / duration, 1);
      const val = Math.round(ease(progress) * target);
      // Formatage : séparateur de milliers
      const formatted = isLarge
        ? val.toLocaleString('en-US').replace(/,/g, '\u202F') // espace fine
        : val;
      el.textContent = formatted + suffix;
      if (progress < 1) requestAnimationFrame(tick);
    }
    requestAnimationFrame(tick);
  }

  function animateBar(el, pct, max) {
    const width = Math.min((pct / max) * 100, 100);
    // Petit délai pour que la transition CSS se déclenche
    setTimeout(() => { el.style.width = width + '%'; }, 50);
  }

  // ── Intersection Observer ──────────────────────────────────────
  function triggerAnimations() {
    root.classList.add('vs-visible');

    // Compteurs
    root.querySelectorAll('[data-target]').forEach(el => {
      const target  = parseFloat(el.dataset.target);
      const suffix  = el.dataset.suffix || '';
      const duration = target >= 10000 ? 1800 : target >= 1000 ? 1500 : 1200;
      animateCounter(el, target, suffix, duration);
    });

    // Barres
    root.querySelectorAll('.vs-bar-fill[data-pct]').forEach(el => {
      animateBar(el, parseFloat(el.dataset.pct), parseFloat(el.dataset.max));
    });
  }

  const observer = new IntersectionObserver(
    (entries) => {
      if (entries[0].isIntersecting) {
        triggerAnimations();
        observer.disconnect();
      }
    },
    { threshold: 0.15 }
  );
  observer.observe(root);

})();
</script>

Weld Australia, the body that represents the welding profession in Australia, projects a shortfall of 70,000 welders by 2030 — and that figure was calculated before the full scale of Australia's renewable energy buildout was factored in. The number of welding trade workers in Australia actually dropped by 8% over recent years, while at the same time Queensland saw welding vacancies increase by 87% and Western Australia saw an increase of 80%.

The mining industry's skills shortage increased from 34% in 2021 to 63% in 2022, giving it the second most acute skills shortage of all Australian industries. By mid-2025, vacancies in the mining sector were surpassing the peaks recorded during the 2011–12 mining boom — a boom that was already considered extreme by historical standards.

<div id="viz-gap" style="width:100%; max-width:720px; margin:32px auto;"></div>

<script src="https://cdnjs.cloudflare.com/ajax/libs/d3/7.8.5/d3.min.js"></script>
<script>
(function () {

  const C = {
    bg:       '#0B0F14',
    bgCard:   '#111820',
    accent:   '#F2DBC2',
    white:    '#FFFFFF',
    textSub:  'rgba(255,255,255,0.50)',
    border:   'rgba(255,255,255,0.07)',
    grid:     'rgba(255,255,255,0.05)',
    supply:   '#5a9e72',
    demand:   '#c85a5a',
    gap:      'rgba(200,90,90,0.13)',
    milestone:'rgba(242,219,194,0.18)',
  };

  const years = [2021, 2022, 2023, 2024, 2025, 2026, 2027, 2028, 2029, 2030, 2031, 2032, 2033];

  const supplyData = [
    { year: 2021, val: 100 },
    { year: 2022, val: 97  },
    { year: 2023, val: 95  },
    { year: 2024, val: 93  },
    { year: 2025, val: 92  },  // −8% acté
    { year: 2026, val: 91  },  // extrapolé : poursuite du déclin
    { year: 2027, val: 91  },
    { year: 2028, val: 91  },  // pas de reprise supportée par les données
    { year: 2029, val: 91  },
    { year: 2030, val: 91  },
    { year: 2031, val: 91  },
    { year: 2032, val: 91  },
    { year: 2033, val: 91  },
  ];

  const demandData = [
    { year: 2021, val: 100 },
    { year: 2022, val: 115 },  // skills shortage 34%→63%
    { year: 2023, val: 128 },
    { year: 2024, val: 142 },
    { year: 2025, val: 158 },  // vacances dépassent le pic 2011-12
    { year: 2026, val: 168 },
    { year: 2027, val: 176 },
    { year: 2028, val: 184 },
    { year: 2029, val: 190 },
    { year: 2030, val: 195 },  // 70 000 shortfall projeté
    { year: 2031, val: 198 },
    { year: 2032, val: 201 },
    { year: 2033, val: 204 },  // +56 000 workers needed by 2033
  ];

  const milestones = [
    { year: 2022, label: 'Skills shortage\n34% → 63%',    side: 'above' },
    { year: 2025, label: 'Vacancies exceed\n2011–12 peak', side: 'above' },
    { year: 2030, label: '70,000\nshortfall',              side: 'above' },
  ];

  const container = document.getElementById('viz-gap');
  const totalW    = Math.min(container.offsetWidth || 720, 720);
  const margin    = { top: 60, right: 28, bottom: 56, left: 52 };
  const innerW    = totalW - margin.left - margin.right;
  const innerH    = 280;
  const totalH    = innerH + margin.top + margin.bottom;

  container.style.cssText = `
    background: ${C.bgCard};
    border: 1px solid ${C.border};
    border-radius: 4px;
    overflow: hidden;
    box-sizing: border-box;
    font-family: Arial, sans-serif;
  `;

  const svg = d3.select('#viz-gap')
    .append('svg')
    .attr('width', '100%')
    .attr('viewBox', `0 0 ${totalW} ${totalH}`)
    .attr('preserveAspectRatio', 'xMidYMid meet');

  svg.append('text')
    .attr('x', 16).attr('y', 18)
    .attr('fill', C.accent)
    .attr('font-size', 10).attr('font-weight', '700')
    .attr('letter-spacing', '0.1em')
    .attr('font-family', 'Arial, sans-serif')
    .text('SUPPLY vs DEMAND  ·  WELDERS IN AUSTRALIA  ·  2021–2033');

  svg.append('text')
    .attr('x', 16).attr('y', 34)
    .attr('fill', C.textSub)
    .attr('font-size', 9).attr('font-family', 'Arial, sans-serif')
    .text('Index 100 = 2021 baseline  ·  Supply: Weld Australia  ·  Demand: AUSMASA + Australian Govt  ·  2026–2033 projected');

  const g = svg.append('g')
    .attr('transform', `translate(${margin.left},${margin.top})`);

  const xScale = d3.scaleLinear().domain([2021, 2033]).range([0, innerW]);
  const yScale = d3.scaleLinear().domain([75, 215]).range([innerH, 0]);

  [80, 90, 100, 110, 120, 140, 160, 180, 200].forEach(tick => {
    g.append('line')
      .attr('x1', 0).attr('x2', innerW)
      .attr('y1', yScale(tick)).attr('y2', yScale(tick))
      .attr('stroke', tick === 100 ? 'rgba(255,255,255,0.12)' : C.grid)
      .attr('stroke-width', tick === 100 ? 1.5 : 1)
      .attr('stroke-dasharray', tick === 100 ? '4,3' : 'none');
  });

  g.append('text')
    .attr('x', 3).attr('y', yScale(100) - 4)
    .attr('fill', 'rgba(255,255,255,0.3)').attr('font-size', 7.5)
    .attr('font-family', 'Arial, sans-serif')
    .text('2021 baseline');

  years.forEach(y => {
    g.append('line')
      .attr('x1', xScale(y)).attr('x2', xScale(y))
      .attr('y1', 0).attr('y2', innerH)
      .attr('stroke', C.grid).attr('stroke-width', 1);
  });

  const xProj = xScale(2025.5);
  g.append('line')
    .attr('x1', xProj).attr('x2', xProj)
    .attr('y1', 0).attr('y2', innerH)
    .attr('stroke', 'rgba(242,219,194,0.20)').attr('stroke-width', 1)
    .attr('stroke-dasharray', '4,3');

  g.append('text')
    .attr('x', xProj + 5).attr('y', innerH - 6)
    .attr('fill', 'rgba(242,219,194,0.35)').attr('font-size', 7.5)
    .attr('font-family', 'Arial, sans-serif')
    .text('← Measured  ·  Projected →');

  const areaGen = d3.area()
    .x(d => xScale(d.year))
    .y0(d => { const s = supplyData.find(s => s.year === d.year); return yScale(s ? s.val : 100); })
    .y1(d => yScale(d.val))
    .curve(d3.curveCatmullRom.alpha(0.5));

  g.append('path').datum(demandData).attr('fill', C.gap).attr('d', areaGen);

  const gapLabelYear = 2028;
  const dVal = demandData.find(d => d.year === gapLabelYear).val;
  const sVal = supplyData.find(d => d.year === gapLabelYear).val;
  const gapMid = (dVal + sVal) / 2;

  g.append('text')
    .attr('x', xScale(gapLabelYear)).attr('y', yScale(gapMid) + 4)
    .attr('text-anchor', 'middle')
    .attr('fill', 'rgba(200,90,90,0.45)')
    .attr('font-size', 9.5).attr('font-weight', '700')
    .attr('font-family', 'Arial, sans-serif')
    .attr('letter-spacing', '0.12em')
    .text('SHORTAGE GAP');

  const lineGen = d3.line()
    .x(d => xScale(d.year))
    .y(d => yScale(d.val))
    .curve(d3.curveCatmullRom.alpha(0.5));

  g.append('path').datum(supplyData).attr('fill', 'none').attr('stroke', C.supply).attr('stroke-width', 2.5).attr('d', lineGen);
  g.append('path').datum(demandData).attr('fill', 'none').attr('stroke', C.demand).attr('stroke-width', 2.5).attr('d', lineGen);

  const demandProjected = demandData.filter(d => d.year >= 2025);
  const supplyProjected = supplyData.filter(d => d.year >= 2025);

  g.append('path').datum(demandProjected).attr('fill', 'none').attr('stroke', C.demand).attr('stroke-width', 2).attr('stroke-dasharray', '5,4').attr('opacity', 0.7).attr('d', lineGen);
  g.append('path').datum(supplyProjected).attr('fill', 'none').attr('stroke', C.supply).attr('stroke-width', 2).attr('stroke-dasharray', '5,4').attr('opacity', 0.7).attr('d', lineGen);

  milestones.forEach(m => {
    const x  = xScale(m.year);
    const dV = demandData.find(d => d.year === m.year).val;
    const y  = yScale(dV);
    const lines = m.label.split('\n');

    g.append('line').attr('x1', x).attr('x2', x).attr('y1', y - 6).attr('y2', y - 28).attr('stroke', C.demand).attr('stroke-width', 1).attr('opacity', 0.5);
    g.append('circle').attr('cx', x).attr('cy', y).attr('r', 4).attr('fill', C.demand).attr('opacity', 0.9);

    lines.forEach((line, i) => {
      g.append('text')
        .attr('x', x).attr('y', y - 32 - (lines.length - 1 - i) * 13)
        .attr('text-anchor', 'middle')
        .attr('fill', 'rgba(200,90,90,0.75)')
        .attr('font-size', 7.5).attr('font-family', 'Arial, sans-serif')
        .attr('font-weight', i === 0 ? '700' : '400')
        .text(line);
    });
  });

  function endDot(data, color, labelFn) {
    const last = data[data.length - 1];
    g.append('circle').attr('cx', xScale(last.year)).attr('cy', yScale(last.val)).attr('r', 4).attr('fill', color).attr('opacity', 0.9);
    g.append('text').attr('x', xScale(last.year) + 7).attr('y', yScale(last.val) + 4).attr('fill', color).attr('font-size', 8.5).attr('font-weight', '700').attr('font-family', 'Arial, sans-serif').text(labelFn(last.val));
  }

  endDot(demandData, C.demand, v => '+' + Math.round(v - 100) + ' index');
  endDot(supplyData, C.supply, v => (v < 100 ? '' : '+') + Math.round(v - 100) + ' index');

  const xAxis = d3.axisBottom(xScale).tickValues(years).tickFormat(d => d.toString()).tickSize(4);
  const xG = g.append('g').attr('transform', `translate(0,${innerH})`).call(xAxis);
  xG.select('.domain').attr('stroke', C.border);
  xG.selectAll('text').attr('fill', C.textSub).attr('font-size', 8).attr('font-family', 'Arial, sans-serif');
  xG.selectAll('.tick line').attr('stroke', C.border);

  const yAxis = d3.axisLeft(yScale).tickValues([80, 90, 100, 120, 140, 160, 180, 200]).tickFormat(d => d).tickSize(4);
  const yG = g.append('g').call(yAxis);
  yG.select('.domain').attr('stroke', C.border);
  yG.selectAll('text').attr('fill', C.textSub).attr('font-size', 8).attr('font-family', 'Arial, sans-serif');
  yG.selectAll('.tick line').attr('stroke', C.border);

  g.append('text').attr('transform', 'rotate(-90)').attr('x', -innerH / 2).attr('y', -40).attr('text-anchor', 'middle').attr('fill', C.textSub).attr('font-size', 8.5).attr('font-family', 'Arial, sans-serif').text('Index (2021 = 100)');

  const legEntries = [
    { color: C.supply, label: 'Certified welder supply' },
    { color: C.demand, label: 'Industry demand (vacancies + projections)' },
  ];

  const legX = 8, legY = innerH - 42;
  g.append('rect').attr('x', legX - 6).attr('y', legY - 8).attr('width', 272).attr('height', legEntries.length * 19 + 14).attr('fill', 'rgba(11,15,20,0.82)').attr('stroke', C.border).attr('stroke-width', 1).attr('rx', 3);

  legEntries.forEach((d, i) => {
    const ly = legY + i * 19 + 4;
    g.append('line').attr('x1', legX).attr('x2', legX + 20).attr('y1', ly + 5).attr('y2', ly + 5).attr('stroke', d.color).attr('stroke-width', 2.5);
    g.append('text').attr('x', legX + 26).attr('y', ly + 9).attr('fill', C.white).attr('font-size', 8.5).attr('font-family', 'Arial, sans-serif').text(d.label);
  });

  const tooltip  = svg.append('g').attr('opacity', 0);
  const ttBg     = tooltip.append('rect').attr('rx', 3).attr('fill', 'rgba(11,15,20,0.94)').attr('stroke', C.border).attr('stroke-width', 1);
  const ttYear   = tooltip.append('text').attr('fill', C.accent).attr('font-size', 9).attr('font-weight', '700').attr('font-family', 'Arial, sans-serif');
  const ttSupply = tooltip.append('text').attr('fill', C.supply).attr('font-size', 9).attr('font-family', 'Arial, sans-serif');
  const ttDemand = tooltip.append('text').attr('fill', C.demand).attr('font-size', 9).attr('font-family', 'Arial, sans-serif');
  const ttGap    = tooltip.append('text').attr('fill', 'rgba(200,90,90,0.75)').attr('font-size', 8.5).attr('font-family', 'Arial, sans-serif');

  const cursorLine = g.append('line').attr('y1', 0).attr('y2', innerH).attr('stroke', 'rgba(255,255,255,0.15)').attr('stroke-width', 1).attr('opacity', 0);
  const dotS = g.append('circle').attr('r', 4).attr('fill', C.supply).attr('opacity', 0);
  const dotD = g.append('circle').attr('r', 4).attr('fill', C.demand).attr('opacity', 0);

  function interp(data, year) {
    const sorted = [...data].sort((a, b) => a.year - b.year);
    for (let i = 0; i < sorted.length - 1; i++) {
      if (year >= sorted[i].year && year <= sorted[i + 1].year) {
        const t = (year - sorted[i].year) / (sorted[i + 1].year - sorted[i].year);
        return sorted[i].val + t * (sorted[i + 1].val - sorted[i].val);
      }
    }
    return null;
  }

  svg.on('mousemove', function (event) {
    const [mx, my] = d3.pointer(event);
    const xInner = mx - margin.left;
    if (xInner < 0 || xInner > innerW) {
      tooltip.attr('opacity', 0); cursorLine.attr('opacity', 0);
      dotS.attr('opacity', 0); dotD.attr('opacity', 0); return;
    }
    const year = xScale.invert(xInner);
    if (year < 2021 || year > 2033) { tooltip.attr('opacity', 0); return; }

    const sV = interp(supplyData, year);
    const dV = interp(demandData, year);
    if (!sV || !dV) return;

    const gap = Math.round(dV - sV);
    const isProjected = year > 2025;

    cursorLine.attr('x1', xInner).attr('x2', xInner).attr('opacity', 1);
    dotS.attr('cx', xInner).attr('cy', yScale(sV)).attr('opacity', 0.9);
    dotD.attr('cx', xInner).attr('cy', yScale(dV)).attr('opacity', 0.9);

    const ttW = 178, ttH = 72;
    let ttX = mx + 12, ttY = my - 40;
    if (ttX + ttW > totalW - 6) ttX = mx - ttW - 12;
    if (ttY < 4) ttY = 4;
    if (ttY + ttH > totalH - 4) ttY = totalH - ttH - 4;

    ttBg.attr('x', ttX).attr('y', ttY).attr('width', ttW).attr('height', ttH);
    ttYear.attr('x', ttX + 9).attr('y', ttY + 14).text(Math.round(year) + (isProjected ? '  (projected)' : ''));
    ttSupply.attr('x', ttX + 9).attr('y', ttY + 29).text('Supply:  ' + Math.round(sV));
    ttDemand.attr('x', ttX + 9).attr('y', ttY + 44).text('Demand: ' + Math.round(dV));
    ttGap.attr('x', ttX + 9).attr('y', ttY + 59).text('Gap: +' + gap + ' index points');
    tooltip.attr('opacity', 1);
  });

  svg.on('mouseleave', function () {
    tooltip.attr('opacity', 0); cursorLine.attr('opacity', 0);
    dotS.attr('opacity', 0); dotD.attr('opacity', 0);
  });

})();
</script>

The Australian government estimates the mining industry will require an additional 56,000 workers by 2033, on top of existing vacancies that remain difficult to fill. Western Australia alone is projected to account for 40% of the nation's resource workforce growth over the next five years.

These are not projections from an industry lobby trying to loosen immigration rules. They come from government workforce data, AUSMASA (the Mining and Automotive Skills Alliance), and the employer associations that are already turning work away because they cannot staff it.

## Why the shortage is structural, not cyclical

The temptation is to read this as a commodity cycle story — boom times, tight labour, it will sort itself out when prices soften. That reading is wrong, and the data makes clear why.

First, the workforce demographics. An aging workforce and a lack of new entrants is widening the generational gap and intensifying the shortage, with many young Australians viewing mining as environmentally damaging or outdated. The pipeline of incoming tradespeople is not large enough to replace attrition, let alone expand capacity.

Second, the demand side is not just cyclical mining. Australia has committed to a renewable energy transition that requires massive fabrication volumes. According to the Australian Energy Market Operator's Integrated System Plan, Australia would need approximately 269 GW of wind and 278 GW of solar to become a renewable energy superpower — equivalent to 34 times its current variable renewable energy capacity. Wind towers, solar mounting structures, grid upgrades, hydrogen production facilities — all of it requires welders, and none of that demand disappears when iron ore prices dip.

Third, immigration is not a straightforward fix. There is no magic pool of international welders from which to draw — this is a global skills crisis. The shortage is not Australian. It is a structural problem in every developed industrial economy simultaneously, driven by the same generational shift away from trades and the same collapse in apprenticeship completions.

The cost of placing a migrant worker in Australia can reach AUD 20,000 per candidate when training, licensing, equipment, and visa sponsorship costs are factored in, which makes the process prohibitive for smaller contractors and creates a high bar for overseas tradespeople who want to work there without being sponsored.

## What automation changes and what it does not

Autonomous haulage accounted for more than half of haul truck movements in the Pilbara by mid-2025. Epiroc converted all 78 haul trucks at Roy Hill to autonomous operation. BHP introduced battery-electric trucks at Jimblebar. The automation of haulage, drilling, and rail systems is real and accelerating.

None of it replaces a welder.

Autonomous haulage removes the driver. It does not fabricate the truck's wear components, does not repair the crusher frame after it cracks, does not rebuild the conveyor chute that has been eaten through by abrasive ore. Every autonomous truck still has a structural frame made of high-strength steel. Every conveyor in the Pilbara still has AR400 liner plates and chrome-moly pivot pins. The automation of haulage logistics increases the throughput of mining operations, which increases the load on the fixed infrastructure, which increases the maintenance demand on the fabrication and repair side.

The more tonnes moved per hour, the faster equipment wears. The faster equipment wears, the more certified welders you need.

## Where this leaves a certified welder

Welders in the Australian mining sector earn between AUD 140,000 and AUD 200,000 annually, with FIFO rates in Western Australia regularly pushing the upper end of that range. Welder (ANZSCO 322313) sits in the Core Skills Stream of the Skills in Demand visa, meaning employers can sponsor overseas welders when they cannot find qualified local candidates. For those who want permanent residency without sponsorship, the Subclass 189 independent skilled visa is accessible through a TRA (Trades Recognition Australia) assessment, and welding is on the Medium and Long-term Strategic Skills List.

<!-- ============================================================
     Visualisation : Timeline — Pathway TRA → Subclass 189
     Article : "Why Australia's Mining Boom Is Creating a Critical Shortage"
     Blog : wallid-guergour.com | Jekyll / GitHub Pages
     Pas de dépendance externe — HTML/CSS/JS pur
     Déclenchement : Intersection Observer (animation au scroll)
     ============================================================ -->

<div id="viz-pathway" style="width:100%; max-width:720px; margin:32px auto;"></div>

<style>
#viz-pathway {
  --bg:      #0B0F14;
  --card:    #0f161e;
  --accent:  #F2DBC2;
  --border:  rgba(255,255,255,0.07);
  --sub:     rgba(255,255,255,0.42);
  --dim:     rgba(255,255,255,0.18);
  --green:   #5a9e72;
  --amber:   #c8964a;
  --red:     #c85a5a;
  --track:   rgba(255,255,255,0.06);
  font-family: Arial, sans-serif;
  background: var(--bg);
  border: 1px solid var(--border);
  border-radius: 4px;
  overflow: hidden;
  box-sizing: border-box;
}

/* ── En-tête ───────────────────────────────────────────────── */
#viz-pathway .vp-header {
  padding: 16px 22px 12px;
  border-bottom: 1px solid var(--border);
  display: flex;
  align-items: baseline;
  gap: 14px;
  flex-wrap: wrap;
}
#viz-pathway .vp-header-label {
  font-size: 10px;
  letter-spacing: 0.13em;
  text-transform: uppercase;
  color: var(--accent);
  font-weight: 700;
  font-family: 'Courier New', monospace;
}
#viz-pathway .vp-header-sub {
  font-size: 9px;
  color: var(--sub);
  letter-spacing: 0.01em;
}

/* ── Corps ─────────────────────────────────────────────────── */
#viz-pathway .vp-body {
  padding: 28px 24px 24px;
}

/* ── Rail horizontal ───────────────────────────────────────── */
#viz-pathway .vp-rail-wrap {
  position: relative;
  margin-bottom: 0;
}

#viz-pathway .vp-rail {
  position: relative;
  height: 2px;
  background: var(--track);
  margin: 0 28px;
}

#viz-pathway .vp-rail-fill {
  position: absolute;
  top: 0; left: 0;
  height: 100%;
  width: 0%;
  background: linear-gradient(90deg, var(--green), var(--accent));
  transition: width 1.6s cubic-bezier(0.16, 1, 0.3, 1);
  border-radius: 2px;
}

/* ── Étapes ────────────────────────────────────────────────── */
#viz-pathway .vp-steps {
  display: flex;
  justify-content: space-between;
  position: relative;
  margin-top: 0;
}

#viz-pathway .vp-step {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  position: relative;
  cursor: default;
}

/* Nœud sur le rail */
#viz-pathway .vp-node {
  width: 18px;
  height: 18px;
  border-radius: 50%;
  border: 2px solid var(--dim);
  background: var(--card);
  display: flex;
  align-items: center;
  justify-content: center;
  margin-top: -9px;
  transition: border-color 0.4s, background 0.4s;
  z-index: 2;
  position: relative;
}

#viz-pathway .vp-node-inner {
  width: 6px;
  height: 6px;
  border-radius: 50%;
  background: var(--dim);
  transition: background 0.4s, transform 0.4s;
  transform: scale(0);
}

/* Contenu sous le nœud */
#viz-pathway .vp-step-content {
  margin-top: 16px;
  padding: 0 6px;
  text-align: center;
  width: 100%;
  opacity: 0;
  transform: translateY(6px);
  transition: opacity 0.5s ease, transform 0.5s ease;
}

/* Numéro d'étape */
#viz-pathway .vp-step-num {
  font-size: 9px;
  letter-spacing: 0.1em;
  color: var(--dim);
  text-transform: uppercase;
  font-family: 'Courier New', monospace;
  transition: color 0.4s;
}

/* Titre */
#viz-pathway .vp-step-title {
  font-size: 12px;
  font-weight: 700;
  color: rgba(255,255,255,0.85);
  margin-top: 5px;
  line-height: 1.3;
  letter-spacing: 0.01em;
}

/* Durée badge */
#viz-pathway .vp-step-duration {
  display: inline-block;
  margin-top: 7px;
  padding: 2px 8px;
  border-radius: 2px;
  font-size: 9px;
  font-weight: 700;
  font-family: 'Courier New', monospace;
  letter-spacing: 0.06em;
  border: 1px solid transparent;
}

/* Description */
#viz-pathway .vp-step-desc {
  margin-top: 7px;
  font-size: 10px;
  color: var(--sub);
  line-height: 1.5;
  padding: 0 4px;
}

/* Tags de conditions */
#viz-pathway .vp-tags {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 4px;
  margin-top: 8px;
}
#viz-pathway .vp-tag {
  font-size: 8px;
  padding: 2px 6px;
  border-radius: 2px;
  background: rgba(255,255,255,0.05);
  border: 1px solid rgba(255,255,255,0.1);
  color: var(--sub);
  letter-spacing: 0.04em;
}

/* Connecteur durée entre étapes */
#viz-pathway .vp-connector {
  position: absolute;
  top: -24px;
  left: 50%;
  transform: translateX(-50%);
  font-size: 7.5px;
  color: var(--dim);
  letter-spacing: 0.06em;
  white-space: nowrap;
  font-family: 'Courier New', monospace;
  opacity: 0;
  transition: opacity 0.5s ease 0.8s;
}

/* Ligne totale en bas */
#viz-pathway .vp-total {
  margin-top: 28px;
  padding-top: 16px;
  border-top: 1px solid var(--border);
  display: flex;
  justify-content: space-between;
  align-items: center;
  flex-wrap: wrap;
  gap: 8px;
  opacity: 0;
  transition: opacity 0.5s ease 1.2s;
}
#viz-pathway .vp-total-label {
  font-size: 9px;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: var(--sub);
  font-family: 'Courier New', monospace;
}
#viz-pathway .vp-total-val {
  font-size: 22px;
  font-weight: 700;
  font-family: 'Courier New', monospace;
  color: var(--accent);
  letter-spacing: -0.5px;
}
#viz-pathway .vp-total-note {
  font-size: 9px;
  color: var(--sub);
  line-height: 1.5;
  max-width: 340px;
  text-align: right;
}

/* ── Animations d'entrée ───────────────────────────────────── */
#viz-pathway.vp-visible .vp-step-content {
  opacity: 1;
  transform: translateY(0);
}
#viz-pathway.vp-visible .vp-connector {
  opacity: 1;
}
#viz-pathway.vp-visible .vp-total {
  opacity: 1;
}
</style>

<script>
(function () {

  const root = document.getElementById('viz-pathway');

  // ── Données des étapes ────────────────────────────────────────
  const steps = [
    {
      num:      '01',
      title:    'TRA Skills Assessment',
      duration: '3 – 6 months',
      dColor:   'rgba(90,158,114,0.2)',
      dBorder:  'rgba(90,158,114,0.5)',
      dText:    '#5a9e72',
      desc:     'Submit trade evidence, employment references, and qualification docs to Trades Recognition Australia. Assessment can be done from Canada.',
      tags:     ['CWB / AWS accepted', 'Done from overseas', 'No employer needed'],
      nodeColor:'#5a9e72',
    },
    {
      num:      '02',
      title:    'Points Test & EOI',
      duration: '1 – 4 weeks',
      dColor:   'rgba(200,150,74,0.15)',
      dBorder:  'rgba(200,150,74,0.45)',
      dText:    '#c8964a',
      desc:     'Submit Expression of Interest via SkillSelect. Score is calculated on age, English, experience, qualifications. Welding is on the MLTSSL.',
      tags:     ['Age points', 'English test', 'Years of experience'],
      nodeColor:'#c8964a',
    },
    {
      num:      '03',
      title:    'Invitation to Apply',
      duration: '1 – 12 months',
      dColor:   'rgba(200,150,74,0.15)',
      dBorder:  'rgba(200,150,74,0.45)',
      dText:    '#c8964a',
      desc:     'Wait for an invitation round. Timing depends on your points score vs the pool. Higher scores get invited first. Welding draws happen regularly.',
      tags:     ['Points-based', 'No sponsorship req.', 'Competitive pool'],
      nodeColor:'#c8964a',
    },
    {
      num:      '04',
      title:    'Subclass 189 — PR',
      duration: '6 – 14 months',
      dColor:   'rgba(242,219,194,0.12)',
      dBorder:  'rgba(242,219,194,0.4)',
      dText:    '#F2DBC2',
      desc:     'Lodge full visa application within 60 days of invitation. Once granted: permanent residency, no employer restriction, work anywhere in Australia.',
      tags:     ['Permanent residency', 'No employer ties', 'Full work rights'],
      nodeColor:'#F2DBC2',
    },
  ];

  // ── En-tête ───────────────────────────────────────────────────
  const header = document.createElement('div');
  header.className = 'vp-header';
  header.innerHTML = `
    <span class="vp-header-label">Visa Pathway</span>
    <span class="vp-header-sub">TRA Skills Assessment → Subclass 189 Permanent Residence · Estimated timeline for a certified welder applying from overseas</span>
  `;
  root.appendChild(header);

  // ── Corps ─────────────────────────────────────────────────────
  const body = document.createElement('div');
  body.className = 'vp-body';
  root.appendChild(body);

  // Rail
  const railWrap = document.createElement('div');
  railWrap.className = 'vp-rail-wrap';

  const rail = document.createElement('div');
  rail.className = 'vp-rail';
  const railFill = document.createElement('div');
  railFill.className = 'vp-rail-fill';
  rail.appendChild(railFill);
  railWrap.appendChild(rail);
  body.appendChild(railWrap);

  // Étapes
  const stepsRow = document.createElement('div');
  stepsRow.className = 'vp-steps';
  body.appendChild(stepsRow);

  steps.forEach((s, i) => {
    const step = document.createElement('div');
    step.className = 'vp-step';

    // Nœud
    const node = document.createElement('div');
    node.className = 'vp-node';
    node.dataset.color = s.nodeColor;
    const inner = document.createElement('div');
    inner.className = 'vp-node-inner';
    inner.dataset.color = s.nodeColor;
    node.appendChild(inner);
    step.appendChild(node);

    // Contenu
    const content = document.createElement('div');
    content.className = 'vp-step-content';
    content.style.transitionDelay = (i * 120 + 200) + 'ms';

    const tagsHtml = s.tags.map(t => `<span class="vp-tag">${t}</span>`).join('');

    content.innerHTML = `
      <div class="vp-step-num">${s.num}</div>
      <div class="vp-step-title">${s.title}</div>
      <div class="vp-step-duration" style="
        background:${s.dColor};
        border-color:${s.dBorder};
        color:${s.dText};
      ">${s.duration}</div>
      <div class="vp-step-desc">${s.desc}</div>
      <div class="vp-tags">${tagsHtml}</div>
    `;
    step.appendChild(content);

    stepsRow.appendChild(step);
  });

  // Ligne totale
  const total = document.createElement('div');
  total.className = 'vp-total';
  total.innerHTML = `
    <div>
      <div class="vp-total-label">Total estimated timeline</div>
      <div class="vp-total-val">12 – 36 months</div>
    </div>
    <div class="vp-total-note">
      From first TRA submission to PR grant. The TRA assessment can be completed<br>
      from Canada. No employer sponsorship required at any stage.
    </div>
  `;
  body.appendChild(total);

  // ── Animation au scroll ───────────────────────────────────────
  function triggerAnimation() {
    root.classList.add('vp-visible');

    // Rail fill
    setTimeout(() => { railFill.style.width = '100%'; }, 100);

    // Nœuds — allumage séquentiel
    const nodes  = root.querySelectorAll('.vp-node');
    const inners = root.querySelectorAll('.vp-node-inner');
    nodes.forEach((node, i) => {
      const color = node.dataset.color;
      setTimeout(() => {
        node.style.borderColor = color;
        node.style.boxShadow   = `0 0 8px ${color}55`;
        inners[i].style.background = color;
        inners[i].style.transform  = 'scale(1)';
      }, 300 + i * 180);
    });
  }

  const observer = new IntersectionObserver(
    entries => {
      if (entries[0].isIntersecting) {
        triggerAnimation();
        observer.disconnect();
      }
    },
    { threshold: 0.15 }
  );
  observer.observe(root);

})();
</script>

The visa pathway and the market demand exist in parallel. The industry is not asking whether it wants overseas certified tradespeople. It is asking why more of them are not coming.

The answer, from where I sit in Quebec, is partly information and partly preparation. The TRA assessment process is not difficult for a welder with real certifications and documented work history — CWB, AWS, ASME — but it requires knowing the process, understanding how Canadian and French qualifications map to Australian standards, and building the documentation package correctly.

The opportunity is real. Australia's mining industry is in the middle of a major boom, and the fabrication and maintenance demand that comes with it is not going away in this decade. For a certified industrial welder with experience on high-strength steels and mining equipment, the question is not whether there is a place in that market. The question is how to get there with the right paperwork in hand.

That is the question I am working on answering for myself, and I will document the process here as it moves forward.
